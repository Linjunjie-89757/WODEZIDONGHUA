# 2026-06-07 Case CRUD Smoke

## Scope

- New frontend: `D:\CodeProject\AutoTestHub`
- Old backend: `D:\CodeProject\auto\server`
- Frontend URL: `http://localhost:5173`
- Backend base URL: `http://localhost:8080/api`
- Module route: `/case-center`

This smoke verifies basic case create, edit, delete, and list refresh. It does not include directory management, batch actions, execution, review, attachments, or AI generation flows.

## Scenario

| Step | Status | Notes |
| --- | --- | --- |
| Login | Pass | `POST /api/auth/login` returned 200. Initial `GET /api/auth/me` returned 401 before login, which is expected. |
| Load workspace options | Pass | `GET /api/workspaces/switchable` returned 200. |
| Load readonly case center | Pass | `GET /api/cases/directories` and `GET /api/cases?pageNo=1&pageSize=20` returned 200. |
| Create case | Pass | Created `smoke-case-20260606171247`; `POST /api/cases` returned 200. |
| Refresh after create | Pass | Case list refreshed and the created case appeared. |
| Open edit with detail preload | Pass | `GET /api/cases/134` returned 200 and precondition, steps, and expected result were preloaded into the edit dialog. |
| Edit case | Pass | Renamed to `smoke-case-20260606171247-edited`; `PUT /api/cases/134` returned 200. |
| Delete case | Pass | `DELETE /api/cases/134` returned 200. |
| Refresh after delete | Pass | Test case was no longer visible. |

## Requests Observed

- `GET /api/auth/me`: 401 before login.
- `POST /api/auth/login`: 200.
- `GET /api/workspaces/switchable`: 200.
- `GET /api/cases/directories`: 200.
- `GET /api/cases?pageNo=1&pageSize=20`: 200.
- `DELETE /api/cases/133`: 200 cleanup from first failing detail-preload smoke.
- `POST /api/cases`: 200.
- `GET /api/cases/134`: 200.
- `PUT /api/cases/134`: 200.
- `DELETE /api/cases/134`: 200.

## Evidence

- Final screenshot: `output/playwright/case-crud-20260606171247.png`
- Smoke script: `scripts/smoke-case-crud.mjs`
- Final state: smoke test case was removed.

## Result

Pass. Basic case create, detail preload for edit, update, delete, and list refresh are verified against the old backend.
