# 2026-06-07 Bug CRUD Smoke

## Scope

- New frontend: `D:\CodeProject\AutoTestHub`
- Old backend: `D:\CodeProject\auto\server`
- Frontend URL: `http://localhost:5173`
- Backend base URL: `http://localhost:8080/api`
- Module route: `/bug-management`

This smoke verifies basic bug create, edit, detail preload, and list refresh. It does not include bug delete because `BugController.java` does not expose a delete endpoint. It also does not include status transition, assign shortcut, comments, attachments, or source-based creation.

## Scenario

| Step | Status | Notes |
| --- | --- | --- |
| Login | Pass | `POST /api/auth/login` returned 200. Initial `GET /api/auth/me` returned 401 before login, which is expected. |
| Load workspace options | Pass | `GET /api/workspaces/switchable` returned 200. |
| Load readonly bug management | Pass | `GET /api/bugs/statistics` and `GET /api/bugs` returned 200. |
| Load users for assignee select | Pass | `GET /api/users` returned 200. |
| Create bug | Pass | Created `smoke-bug-20260606183704`; `POST /api/bugs` returned 200. |
| Refresh after create | Pass | Bug list refreshed and the created bug appeared. |
| Open edit with detail preload | Pass | `GET /api/bugs/1004` returned 200 and description preloaded into the edit dialog. |
| Edit bug | Pass | Renamed to `smoke-bug-20260606183704-edited`; `PUT /api/bugs/1004` returned 200. |
| Open updated detail | Pass | Detail drawer showed the edited description. |

## Requests Expected

- `GET /api/auth/me`: may return 401 before login.
- `POST /api/auth/login`: 200.
- `GET /api/workspaces/switchable`: 200.
- `GET /api/bugs/statistics`: 200.
- `GET /api/bugs`: 200.
- `GET /api/users`: 200.
- `POST /api/bugs`: 200.
- `GET /api/bugs/{id}`: 200.
- `PUT /api/bugs/{id}`: 200.

## Requests Observed

- `GET /api/auth/me`: 401 before login.
- `POST /api/auth/login`: 200.
- `GET /api/workspaces/switchable`: 200.
- `GET /api/bugs/statistics`: 200.
- `GET /api/bugs`: 200.
- `GET /api/users`: 200.
- `POST /api/bugs`: 200.
- `GET /api/bugs/1004`: 200.
- `PUT /api/bugs/1004`: 200.

## Evidence

- Final screenshot: `output/playwright/bug-crud-20260606183704.png`
- Smoke script: `scripts/smoke-bug-crud.mjs`

## Result

Pass. Basic bug create, detail preload for edit, update, detail refresh, and list refresh are verified against the old backend.
